# Kaggle 2x Tesla T4 runtime
transformers>=4.44
accelerate
bitsandbytes
scikit-learn
scipy        # transitive dep of sklearn; imported directly by the CV tests
joblib
datasets
sentencepiece
protobuf
numpy

# Local smoke-testing (CPU, optional) — torch CPU wheel ~200 MB
# torch>=2.2
# tokenizers>=0.19
